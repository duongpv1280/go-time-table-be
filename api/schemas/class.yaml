type: object
properties:
  id:
    type: string
    format: uuid
  name:
    type: string
  grade:
    type: integer
  createdAt:
    type: string
    format: date-time
  updatedAt:
    type: string
    format: date-time
required: [id, name, grade, createdAt, updatedAt]
